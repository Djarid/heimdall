//! Sink contracts, the effect-primitive taxonomy and D81 validation for the
//! `boundary-gjoll` re-expression (D109, spec section 5.1). Placeholder only: real
//! content (`EffectPrimitive`, `SinkDeclaration`, `SinkRegistry`,
//! `ValidationOutcome`, `validate_proposal`, `intrinsically_consequential`) lands in
//! a later build (issue #17/#18). This file exists now only so
//! `crates/boundary-gjoll` is a buildable skeleton per issue #16.
