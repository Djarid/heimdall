//! The pure rule core (layer one) for the `boundary-gjoll` re-expression (D109,
//! spec section 5.1). Placeholder only: real content (`ConsequentialityVerdict`,
//! `apply`) lands in a later build (issue #17/#18). This file exists now only so
//! `crates/boundary-gjoll` is a buildable skeleton per issue #16.
