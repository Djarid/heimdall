// TODO: `action_critical_for` (the only reader of
// `hierarchy_vor::CohortSurface::consequential_sinks`), the translation into
// `boundary_gjoll`'s input shapes, the single
// `boundary_gjoll::consequentiality::evaluate` call site, and the mapping of
// `GateDecision` to a `CheckOutcome` (REQ-15, REQ-17, REQ-19). A later phase
// of this same issue (`.opencode/plans/himinbjorg-step-three.md` section 14
// execution workflow step 3.4, section 13 file 9). Empty stub for now: the
// crate does not yet export `gate_bridge::action_critical_for` or
// `gate_bridge::evaluate_taint_compatibility`.
