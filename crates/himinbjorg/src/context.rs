// TODO: `build_context` and the fixed context's own hardcoded constants
// (target scope, constraint set, blast-radius bound, resource ceiling) plus
// their compile-time non-emptiness assertions (REQ-7, REQ-8, REQ-12 to
// REQ-14, REQ-16). A later phase of this same issue
// (`.opencode/plans/himinbjorg-step-three.md` section 14 execution workflow
// step 3.3, section 13 file 6). Empty stub for now: the crate does not yet
// export `build_context`.
