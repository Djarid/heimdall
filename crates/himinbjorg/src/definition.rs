// TODO: `enforce_definition`, the hardcoded global default action set, the
// set intersection, the byte-equality trust-ceiling check and the
// non-empty-intersection compile-time assertion (REQ-9). A later phase of
// this same issue (`.opencode/plans/himinbjorg-step-three.md` section 14
// execution workflow step 3.3, section 13 file 7). Empty stub for now: the
// crate does not yet export `enforce_definition`.
