"""Heimdall's ontology tree (Yggdrasil) and its deterministic reasoner (Nornir).

This package makes the loaded ontology and Nornir importable as
`ontology.yggdrasil` and `ontology.nornir`. The non-code directories here
(`upper/bfo`, `reference/sumo`, and the layer stubs `spine`, `domain`, `media`,
`rules`, `tests`) are the on-disk tree documented in `ontology/README.md`; the
runnable authored layers live in the `yggdrasil` subpackage, and the reasoner in
`nornir`. See `ONTOLOGY_CONSTRUCTION.md` for the methodology.
"""
