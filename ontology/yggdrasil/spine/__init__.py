"""The shared spine: Heimdall-authored types on the BFO upper layer.

Shared across all domains and all media, never per-agent (the per-agent binding is
control-surface state, D20). Three modules: `trust` (the trust lattice), `action`
(the action vocabulary), `constraint` (the constraint vocabulary). See
`ontology/spine/README.md` and `ONTOLOGY_CONSTRUCTION.md` sections 4.1 and 6.
"""
