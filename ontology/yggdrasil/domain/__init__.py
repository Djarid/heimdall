"""Domain layers: one per subject-matter domain, each extending the BFO spine.

A domain is subject-matter, not medium (D22, D22a). A domain type must be
medium-neutral: the same fact types identically whether it arrived by email, web or
social media. The Phase 1 seed is `communications`. Future domains (scheduling,
finance) attach as sibling modules under the shared spine without editing the
existing domains or the spine (the attach test, D29,
`ONTOLOGY_CONSTRUCTION.md` 4.2).
"""
